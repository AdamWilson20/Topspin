<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.topspin">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ViewScores"
            android:label="Live Scores"></activity>
        <activity
            android:name=".ScoreByGameSingles"
            android:label="Scoring Singles"/>
        <activity
            android:name=".ScoreByGameDoubles"
            android:label="Scoring Doubles"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScoreByGame" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ModifyTournament" />
        <activity android:name=".ViewScheduleEntry" />
        <activity android:name=".AddScheduleEntry" />
        <activity
            android:name=".PlayerRoster"
            android:label="Roster"/>
        <activity android:name=".AddPlayerEntry" />
        <activity android:name=".ModifyPlayer" />
        <activity
            android:name=".ViewSchedule"
            android:label="Schedule" />
        <activity
            android:name=".RegisterActivity"
            android:label="Register" />
    </application>

</manifest>